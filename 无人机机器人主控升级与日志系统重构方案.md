# 无人机/机器人主控升级与日志系统重构方案

文档编号：`UAV-OAM-UPLOG-001`  
版本：`v0.1-review-draft`  
日期：`2026-08-05`  
状态：`方案评审稿 / 未实施 / 未生产批准`  
适用范围：无人机或机器人FC、Linux主控、RC边缘网关、外设控制器和Cloud OAM平台  

---

## 1. 文档目的

本文给出无人机/机器人主控升级与日志系统的重新设计方案，重点解决：

- Linux主控、FC/MCU和外设固件的统一升级；
- 升级包真实性、完整性、硬件兼容性和防降级；
- A/B启动、健康确认、失败回滚和掉电恢复；
- 多设备、多版本和弱网环境下的升级编排；
- 飞控黑匣子、ROS 2数据、Linux日志和OAM遥测的分层管理；
- 本地有界存储、可靠上传、Cloud持久化确认和证据保全；
- 日志检索、指标告警、事故诊断和审计闭环。

本文是目标方案，不代表现有代码已经实现，也不构成真实飞行或生产放行授权。

---

## 2. 输入基线与现状判断

### 2.1 代码基线

本方案参考以下现有实现：

- 仓库：`D:\Project\tars`
- 分支：`chenxin`
- Commit：`156d0d65be28b9e403a404bce21e09f8eee18a2c`
- 项目目录：`D:\Project\tars\huida_uav`

关键代码包括：

- `src/oam/src/upgrade_manage_main.cpp`
- `src/common/hdversionupdate.hpp`
- `version/image_patch/image_patch.sh`
- `src/oam/src/tiny_log/s_log.hpp`
- `src/oam/src/mc_log_producer_main.cpp`
- `src/util/log_upload.hpp`
- `src/oam/src/wifi_manager.cpp`
- `web/source/ccgi/ccgi_save_upgrade.c`

本方案只基于静态代码分析；未编译、未上板、未执行真实升级和日志上传测试。

### 2.2 可继承能力

现有系统已经积累了以下有价值能力，应优先复用：

- 对MCM、NAV、FCM、ESC、BMS、雷达、相机、提升装置和作业设备等多种模块的升级适配；
- 通过ROS 2 Topic完成升级命令、进度和结果交互；
- 升级前检查飞控ARM状态；
- 设备当前版本、期望版本和模块升级状态管理；
- RK平台Boot镜像A/B分区和回读比较基础；
- FC、NAV、CAN、GPS、雷达、相机、电池和作业数据等丰富日志；
- 本地日志分包、压缩、磁盘清理和上传基础；
- 自研二进制日志Header和部分记录CRC能力。

### 2.3 必须重构的部分

现有实现不应直接作为生产级升级与日志安全底座，主要问题包括：

- 升级包主要使用CRC32，没有生产级数字签名；                 
- FTP传输和凭据随消息传递，存在泄漏与伪造风险；
- 本地下载路径、文件大小和输入边界控制不足；
- Linux A/B缺少完整的Test Boot、健康确认和回滚证据；
- 多模块升级缺少统一Manifest、依赖图和事务边界；
- 缺少Campaign、Canary、暂停、扩圈和自动回滚；
- 日志中存在Wi-Fi和FTP凭据字段；
- 日志上传采用FTP和MD5伴随文件，缺少端到端持久化确认；
- 日志删除缺少P0/P1优先级、Incident保护和Legal Hold；
- 未形成Flight Package、统一Schema目录和Cloud观测闭环；
- 未发现RAUC、SWUpdate、MCUboot、TUF/Uptane、MCAP或OpenTelemetry的正式集成。

---

## 3. 建设目标

### 3.1 功能目标

1. 统一管理Linux主控、FC/MCU和外设升级。
2. 支持远程、现场和离线介质升级。
3. 支持A/B、灰度、暂停、恢复和回滚。
4. 支持设备BOM、硬件版本和软件兼容矩阵。
5. 支持弱网、断网、乱序、重复和掉电恢复。
6. 支持升级全生命周期查询和审计。
7. 支持飞行黑匣子、ROS 2数据、系统日志和OAM遥测。
8. 支持异常触发录制、Flight Package和远程诊断。
9. 支持本地分级保留和Cloud可靠归档。
10. 支持指标、日志、Trace和Incident关联分析。

### 3.2 安全目标

- 未签名或签名无效的升级包不能安装；
- 错误机型、硬件版本或设备身份不能接收升级；
- 已撤销、过期或重放的升级命令不能执行；
- 未满足本地安全条件的设备不能升级；
- 升级失败不能破坏已知良好版本；
- 日志不得包含口令、私钥和完整访问Token；
- 日志和升级操作可关联到设备、版本、操作人和审批记录；
- Cloud、RC、HC和FC分别执行自己的校验，不能只依赖上游结论。

### 3.3 非目标

本方案不直接解决：

- 飞控控制律或导航算法安全性；
- 执行器、整机和适航认证；
- 真实飞行放行；
- 所有历史日志格式立即重写；
- 第一阶段全面微服务化；
- 用OpenTelemetry替代飞控黑匣子或原始传感器文件。

---

## 4. 总体设计原则

### 4.1 安全权威分层

- Cloud决定发布策略、审批、设备范围和Campaign；
- RC Gateway负责可靠传输、边缘缓存和弱网恢复；
- HC Update Orchestrator负责本机编排和外设适配；
- FC/MCU Bootloader负责最终镜像真实性、启动和回滚；
- FC始终保留飞行安全和本地升级前置条件最终决定权。

### 4.2 控制面与数据面分离

升级命令、状态、日志遥测和大文件使用不同逻辑通道：

| 平面 | 内容 | 要求 |
|---|---|---|
| Flight Operation | 飞行操作和控制 | 最高时效与隔离 |
| Management | 升级、配置、证书和维护命令 | 强认证、签名、审计 |
| OAM Telemetry | 健康、指标、事件和Trace | 有界缓存、弱网恢复 |
| Artifact | 固件、MCAP、Flight Package和大日志 | 分片、限速、可恢复 |

### 4.3 保留适配，替换信任层

现有多外设升级和业务日志采集具有较高复用价值。重构重点是：

- 替换FTP、CRC32信任和脚本式安装；
- 增加统一签名、Manifest和兼容矩阵；
- 增加标准状态机、持久化和审计；
- 将现有实现收敛为受控Adapter；
- 将日志包装为标准Artifact和可观测事件。

### 4.4 默认拒绝

以下任一条件异常时默认拒绝升级：

- 身份不可信；
- 签名失败；
- Manifest过期；
- 命令重放；
- 硬件不匹配；
- 当前版本不在允许范围；
- 缺少回滚版本；
- 电源、温度或存储不满足条件；
- 设备处于飞行、作业或不安全状态；
- 审计无法可靠写入。

---

## 5. 目标总体架构

```text
┌──────────────────────────── Cloud OAM ────────────────────────────┐
│ Release / Campaign / Approval / Audit                            │
│ TUF/Uptane Metadata                                              │
│ PostgreSQL + Outbox                                              │
│ S3/MinIO Artifact Store                                          │
│ Prometheus/Loki/Tempo/Grafana                                    │
└──────────────────────────────┬─────────────────────────────────────┘
                               │ HTTPS/mTLS
                               │ Signed Command / Artifact / ACK
┌──────────────────────────────▼─────────────────────────────────────┐
│ RC Edge Gateway                                                   │
│ Device Session / L2 WAL / Artifact Cache / Bandwidth Policy       │
└──────────────────────────────┬─────────────────────────────────────┘
                               │ Management + OAM + Artifact
┌──────────────────────────────▼─────────────────────────────────────┐
│ Linux HC                                                          │
│ Update Orchestrator                                               │
│ ├─ RAUC Installer                                                 │
│ ├─ FC/MCU Adapter                                                  │
│ ├─ CAN/UART/Network Peripheral Adapters                           │
│ └─ Health Confirmation                                            │
│                                                                   │
│ OAM Log Agent                                                     │
│ ├─ Flight Recorder Adapter                                        │
│ ├─ journald/Fluent Bit                                            │
│ ├─ rosbag2/MCAP                                                    │
│ ├─ L1 WAL                                                         │
│ └─ Flight Package Builder                                         │
└──────────────────────────────┬─────────────────────────────────────┘
                               │ Existing approved FC-HC interfaces
┌──────────────────────────────▼─────────────────────────────────────┐
│ FC / MCU / Peripheral Controllers                                 │
│ Signed Bootloader / A-B Slot / Test / Confirm / Rollback          │
│ Lightweight Blackbox / Critical Events                            │
└────────────────────────────────────────────────────────────────────┘
```

---

## 6. 技术选型结论

| 领域 | 首选 | 说明 |
|---|---|---|
| Linux系统升级 | RAUC | 轻量、适合Yocto和U-Boot A/B、职责清晰 |
| Linux Bootloader | U-Boot | Slot、bootcount、bootlimit和回滚 |
| FC/MCU升级 | MCUboot或同等模型 | Test/Confirm/Rollback、防降级、签名 |
| OTA安全元数据 | TUF/Uptane模型 | 离线根、角色签名、过期、防冻结和目标绑定 |
| 多设备升级 | 自研Update Orchestrator | 复用现有CAN/UART/网络Adapter |
| Campaign | 自研模块化服务 | 适应无人机状态、硬件BOM和外设矩阵 |
| Artifact传输 | HTTPS/mTLS | 替代FTP，支持Range和分片恢复 |
| Artifact存储 | S3或MinIO | 内容寻址、生命周期、分片上传 |
| 业务元数据 | PostgreSQL 16 | 设备、发布、Campaign、审计和Outbox |
| 飞控黑匣子 | 现有二进制格式增强 | 避免侵入实时路径 |
| ROS 2数据 | rosbag2 + MCAP | 标准Schema、索引、压缩和回放生态 |
| Linux日志 | systemd-journald | 进程生命周期和结构化字段 |
| 边缘日志采集 | Fluent Bit | 轻量、限速、过滤和有界缓存 |
| 指标与Trace | OpenTelemetry | 统一非实时观测语义 |
| 指标后端 | Prometheus/Mimir | 指标、告警和长期存储 |
| 文本日志 | Loki | 标签和时间查询；复杂全文可选OpenSearch |
| Trace | Tempo | OTel链路关联 |
| 展示 | Grafana | 指标、日志、Trace和Incident关联 |

不建议同时在同一Linux设备使用RAUC和SWUpdate。若硬件无法形成A/B分区，才评估SWUpdate作为替代。

不建议第一阶段直接采用Mender作为总平台，因为多外设升级、飞行状态和本地安全条件仍需大量自定义。

---

## 7. Linux主控升级设计

### 7.1 分区设计

推荐eMMC布局：

```text
boot_a       当前或备用Boot镜像
boot_b       当前或备用Boot镜像
rootfs_a     当前或备用系统
rootfs_b     当前或备用系统
data         配置、设备身份和业务持久化数据
oam_spool    日志WAL、Artifact和下载缓存
rescue       可选的工厂恢复环境
```

要求：

- 当前运行Slot只读或尽量不可变；
- 新版本安装到非当前Slot；
- `data`与系统Slot分离；
- `oam_spool`与飞行关键数据隔离并设置配额；
- U-Boot环境使用冗余存储；
- 不允许脚本根据未校验路径直接执行`dd`；
- RAUC是唯一生产Linux Slot写入入口。

### 7.2 启动与回滚

```text
安装到备用Slot
→ 设置pending
→ 重启
→ Bootloader增加bootcount
→ 新Slot启动
→ 启动HC健康检查
→ 健康通过后mark-good
→ Bootloader清除pending
```

以下情况触发回滚：

- Kernel无法启动；
- RootFS无法挂载；
- Watchdog连续复位；
- 核心驱动加载失败；
- FC-HC通信失败；
- 必需ROS 2节点未进入Ready；
- 数据Schema无法兼容；
- 健康确认超时；
- 人工或Campaign触发回滚。

### 7.3 健康确认

健康检查至少包含：

- Kernel、RootFS和关键设备节点；
- CPU、内存、温度和磁盘；
- FC通信和协议版本；
- 必需ROS 2节点及其心跳；
- 设备身份和证书加载；
- 有效配置与Schema迁移；
- OAM Agent和Update Agent；
- 无连续崩溃或Watchdog复位；
- 当前版本与安装Manifest一致。

健康检查通过只表示新版本能够继续受控验证，不等于无人机可飞。

---

## 8. FC、MCU与外设升级设计

### 8.1 FC/MCU目标模型

```text
Primary Slot       当前运行镜像
Secondary Slot     待验证镜像
Boot Metadata      当前Slot、pending、boot count、security counter
Recovery           最小恢复路径或已知良好版本
```

标准流程：

1. HC下载并验证Artifact。
2. FC/MCU再次验证签名、硬件型号和Security Counter。
3. 分块写Secondary Slot。
4. 每块执行长度和CRC校验。
5. 完整镜像执行SHA-256和签名校验。
6. 设置Test Boot状态。
7. 重启进入新版本。
8. FC完成内部自检。
9. 自检成功后Confirm。
10. 超时、复位或自检失败时自动回滚。

### 8.2 现有外设Adapter改造

将现有升级能力重构为统一接口：

```text
probe()       查询设备、硬件和版本
precheck()    检查模式、电源、连接和兼容性
stage()       传输并暂存Artifact
install()     请求设备安装
verify()      读取实际版本和健康状态
confirm()     确认新版本
rollback()    恢复已知良好版本
status()      返回稳定状态和错误码
cancel()      在安全阶段停止任务
```

Adapter只处理设备协议，不再处理Cloud身份、发布审批和Artifact信任。

### 8.3 Legacy设备

对于暂时不支持签名Bootloader或双Slot的设备：

- 标记为`LEGACY_SINGLE_SLOT`；
- Cloud不得自动全量升级；
- 必须使用更严格的维护窗口和人工审批；
- HC在分发前完成签名和hash验证；
- 升级前导出当前版本和恢复镜像；
- 明确断电后恢复方式；
- 纳入硬件淘汰或Bootloader升级计划。

Legacy兼容不能被描述为达到与签名A/B相同的安全等级。

---

## 9. OTA信任与发布设计

### 9.1 信任角色

| 角色 | 责任 |
|---|---|
| Offline Root | 签署根角色和密钥轮换，离线保存 |
| Targets | 签署正式Artifact和兼容约束 |
| Snapshot | 冻结一次发布涉及的元数据集合 |
| Timestamp | 提供短期新鲜度，防止长期使用旧元数据 |
| Director | 将Release绑定到租户、机型、BOM和设备集合 |

Root密钥不得存放在CI、普通开发机或在线Cloud服务中。

### 9.2 Artifact Manifest

Manifest至少包含：

```text
manifest_version
release_id
artifact_id
component
artifact_type
hardware_model
hardware_revision_range
current_version_constraints
target_version
security_counter
size
sha256
dependencies
installation_order
rollback_artifact
required_mode
not_before
expires_at
key_id
signature_algorithm
signature
```

推荐签名算法为ECDSA P-256。CRC32可以继续用于分块传输，但不能代替SHA-256和数字签名。

### 9.3 供应链

正式Release同时保存：

- 源码Commit；
- 构建环境和构建器身份；
- SBOM；
- 依赖锁定文件；
- 构建日志；
- 测试Evidence Manifest；
- Artifact SHA-256；
- 签名及证书链；
- 审批记录；
- 已知限制和`not_claimed`。

---

## 10. 升级命令与状态机

### 10.1 命令Envelope

升级命令至少包含：

```text
command_id
campaign_id
release_id
tenant_id
asset_id
target_components
issued_at
not_before
expires_at
nonce
expected_inventory_revision
required_mode
approval_refs
policy_version
key_id
signature
```

`command_id`是业务幂等键。网络重试不能生成新的升级任务。

### 10.2 Campaign状态

```text
DRAFT
→ APPROVED
→ SCHEDULED
→ CANARY
→ SOAKING
→ EXPANDING
→ COMPLETED

异常状态：
PAUSED / FAILED / ROLLING_BACK / ROLLED_BACK / CANCELLED
```

### 10.3 单设备状态

```text
ASSIGNED
→ PRECHECKING
→ DOWNLOADING
→ DOWNLOADED
→ VERIFIED
→ STAGED
→ INSTALLING
→ REBOOT_PENDING
→ BOOTED
→ HEALTH_CHECKING
→ CONFIRMED
```

失败状态：

```text
REJECTED
EXPIRED
REPLAY_DETECTED
DOWNLOAD_FAILED
SIGNATURE_FAILED
INCOMPATIBLE
PRECONDITION_FAILED
INSTALL_FAILED
BOOT_FAILED
HEALTH_FAILED
ROLLBACK_FAILED
UNKNOWN
```

`DOWNLOADED`、`INSTALLED`、`BOOTED`和`CONFIRMED`是不同状态，UI和Cloud不能互相推断。

### 10.4 分层回执

Cloud、RC、HC和目标设备分别返回回执：

```text
RECEIVED
VERIFIED
PERSISTED
FORWARDED
ACCEPTED
EXECUTING
SUCCEEDED
REJECTED
FAILED
EXPIRED
ROLLED_BACK
```

每条回执包含：

- `command_id`；
- 处理层级；
- attempt；
- 当前状态；
- 稳定原因码；
- 当前版本和目标版本；
- Boot ID；
- 单调时间和UTC质量；
- 结果hash；
- 前一回执hash。

---

## 11. 升级前置条件

### 11.1 通用条件

- Artifact完整落盘；
- Manifest签名有效；
- Artifact SHA-256正确；
- 目标设备与租户绑定正确；
- 硬件型号和revision匹配；
- 当前版本在允许范围；
- Security Counter不倒退；
- 回滚版本可用；
- 命令未过期且未重放；
- 存储空间满足下载、安装和回滚需求；
- 温度和电源满足阈值；
- 审计可可靠写入。

### 11.2 无人机条件

- FC处于Disarmed；
- 无飞行任务和作业任务；
- 地面状态可信；
- 执行器硬锁或维护模式生效；
- 电池电量、电压和健康度满足策略；
- HC与FC通信正常；
- 不存在更高优先级安全事件；
- 设备处于批准的维护窗口。

仅检查`Disarmed`不足以构成生产升级授权。

---

## 12. 多组件升级事务

一次Release可能包含HC、FC和多个外设。必须显式定义依赖图，例如：

```text
HC Bootloader
  → HC RootFS
      → FC Interface Adapter
          → FC Firmware
              → Peripheral Firmware
```

每个Release声明：

- 安装顺序；
- 可并行组件；
- 不兼容版本组合；
- 中断安全点；
- 回滚顺序；
- 是否允许部分成功；
- 最长允许混合版本时间；
- 人工介入条件。

不应把所有组件伪装成一个跨设备原子事务。正确方式是：

- 每个组件内部具备原子安装或恢复能力；
- Orchestrator管理Saga式阶段和补偿；
- 任一硬门禁失败时暂停后续组件；
- 保留完整混合版本证据；
- 按依赖逆序回滚。

---

## 13. Campaign与灰度发布

灰度不能只按随机百分比，应覆盖硬件和环境矩阵：

```text
SIL/SITL
→ HIL
→ 无桨台架
→ 内部试验机
→ 每种硬件BOM至少一台Canary
→ 单租户小批次
→ 跨租户扩圈
→ 全量
```

扩圈门禁至少包括：

- 下载和验签成功率；
- 安装成功率；
- Boot成功率；
- 健康确认成功率；
- 自动回滚率；
- 新版本崩溃率；
- CPU、内存、温度和功耗变化；
- FC-HC通信错误；
- 飞行关键指标变化；
- 日志缺失率；
- Incident数量和严重度。

出现以下情况立即暂停：

- 签名、硬件匹配或防降级异常；
- 同一硬件BOM连续失败；
- Bootloop或回滚失败；
- FC-HC协议不兼容；
- 关键资源明显回归；
- 跨租户或身份异常；
- 审计链不完整。

---

## 14. 日志分类设计

日志分为四类：

| 类别 | 内容 | 本地格式 | Cloud落点 |
|---|---|---|---|
| Flight Recorder | FC、NAV、控制和高频传感器 | 增强后的现有二进制格式 | 对象存储 |
| ROS Data | ROS 2 Topic、图像、点云和算法数据 | rosbag2/MCAP | 对象存储 |
| System Log | Linux进程、服务和结构化事件 | journald/JSON | Loki/OpenSearch |
| OAM Telemetry | 健康、指标、Trace和计数器 | OTLP | Prometheus/Tempo |

原始飞行数据、视频和MCAP不能进入Loki或Prometheus。

### 14.1 优先级

| 优先级 | 数据 | 保留原则 |
|---|---|---|
| P0 | 崩溃、升级、安全、审计、严重飞控事件 | 未Cloud确认不得主动删除 |
| P1 | Flight Recorder、Incident时间窗 | 长期保护，受控淘汰 |
| P2 | 系统诊断和普通运行日志 | 按容量和时间保留 |
| P3 | 全量ROS数据、Debug、视频和大批量数据 | 最先限速、暂停或淘汰 |

---

## 15. 飞控黑匣子设计

保留现有高效二进制日志，但补充统一文件头和Manifest。

每条记录至少包含：

```text
record_type
schema_version
boot_id
sequence
occurred_mono_ns
occurred_utc
clock_quality
payload_length
payload_crc32
payload
```

每个文件至少包含：

- asset ID；
- FC、HC和外设版本集合；
- 配置hash；
- Boot ID；
- Flight Episode ID；
- 开始/结束单调时间；
- UTC和时间质量；
- Schema目录版本；
- 文件SHA-256；
- 完整/截断状态；
- 掉电恢复索引。

实时线程只执行有界、预分配、非阻塞或受控阻塞写入。压缩、上传、hash和Cloud通信放在HC Agent。

---

## 16. ROS 2与MCAP

ROS 2大数据统一使用`rosbag2 + MCAP`。

每个录制Profile定义：

- Topic allowlist；
- QoS；
- 最大频率；
- 是否降采样；
- 分片大小；
- 压缩算法；
- 单次最大时长；
- 最大磁盘配额；
- 数据分类；
- 触发条件；
- 上传策略。

推荐模式：

```text
Normal：低频摘要和关键Topic
Mission：按Flight Episode录制
Incident：保留触发前循环缓存和触发后时间窗
Diagnostic：人工审批后限时提高采样率
```

MCAP文件必须进入Flight Package，不直接作为OTel日志上传。

---

## 17. Linux系统日志

### 17.1 journald

Linux进程日志统一输出到journald，字段至少包含：

```text
severity
event_code
component
boot_id
asset_id
software_version
config_hash
request_id
trace_id
occurred_mono_ns
message
```

程序只能依赖稳定`event_code`进行告警和统计，不能解析自然语言`message`判断状态。

### 17.2 Fluent Bit

Fluent Bit负责：

- 读取journald；
- 字段白名单；
- 凭据和隐私字段脱敏；
- 按优先级过滤；
- 有界内存和磁盘缓存；
- 上传限速；
- 输出到RC Gateway或Cloud日志入口。

Fluent Bit故障不能阻塞飞行、升级安装或Flight Recorder。

### 17.3 禁止记录

- Wi-Fi密码；
- FTP、SFTP或对象存储密码；
- 私钥；
- 完整Access Token和Refresh Token；
- 未脱敏身份证明；
- 未批准的定位和个人敏感数据；
- 原始升级签名私钥材料。

---

## 18. OpenTelemetry设计

OpenTelemetry用于非实时观测，不进入FC硬实时线程。

建议指标包括：

- CPU、RSS、线程和文件描述符；
- 温度、电压和功耗；
- 磁盘空间和闪存寿命；
- L1/L2 WAL水位；
- 日志产生、压缩、上传和删除数量；
- Artifact积压和上传延迟；
- 升级各阶段耗时；
- 签名失败和防降级拒绝；
- Boot失败、健康失败和回滚次数；
- FC-HC通信错误；
- ROS 2 Topic延迟、频率和丢包；
- CAN、UART和网络错误；
- 证书剩余有效期；
- 时钟同步质量。

禁止把asset ID、飞行ID、错误文本或动态Topic名无控制地用作高基数Metric Label。

---

## 19. Flight Package

一次飞行、一次Incident或一次诊断任务生成一个Flight Package：

```text
flight-package/
├── manifest.json
├── fc/
│   └── blackbox.bin
├── ros/
│   └── episode-001.mcap
├── system/
│   └── journal.json.zst
├── config/
│   └── effective-config.json
├── inventory/
│   └── software-set.json
└── evidence/
    └── incident-events.json
```

Manifest包含：

```text
artifact_id
tenant_id
asset_id
flight_episode_id
boot_ids
software_set
config_hash
start_mono_ns
end_mono_ns
utc_range
clock_quality
files[].logical_path
files[].size
files[].sha256
schema_versions
privacy_class
retention_policy
legal_hold
producer_key_id
signature
```

Cloud必须验证：

- 逻辑路径安全；
- 文件长度；
- 所有SHA-256；
- Manifest签名；
- tenant和asset绑定；
- 数据分类和保留策略；
- 对象存储写入成功。

---

## 20. L1/L2可靠存储与ACK

### 20.1 两级存储

- HC L1：保存结构化OAM事件、关键日志索引和待上传Artifact状态；
- RC L2：保存已从HC接收但尚未被Cloud确认的数据；
- Cloud：PostgreSQL保存元数据，对象存储保存Artifact，观测后端保存指标和文本日志。

### 20.2 ACK语义

```text
PROCESS_ONLY     只进入进程内存，不允许删除上游数据
LOCAL_DURABLE    RC已可靠落盘，HC可删除对应L1数据
CLOUD_DURABLE    Cloud已持久化并验证，RC可删除对应L2数据
```

TCP ACK、HTTP 2xx、QUIC stream ACK或FTP成功返回都不等于Cloud持久化ACK。

### 20.3 去重键

结构化记录建议使用：

```text
asset_id + boot_id + source_id + stream_id + sequence
```

Artifact使用`artifact_id + logical_path + sha256`去重。

---

## 21. 本地存储与满盘策略

建议分目录和配额：

```text
/oam/wal
/oam/artifact
/oam/update
/oam/quarantine
```

初始水位建议：

| 水位 | 动作 |
|---:|---|
| 60% | 趋势告警和容量预测 |
| 75% | 暂停非任务P3录制 |
| 85% | 限制P2并暂停大Artifact |
| 95% | 进入保护状态，仅允许P0/P1和必要升级回滚 |

最终阈值必须通过真实吞吐、断网时长和闪存寿命测试冻结。

删除顺序：

1. 已Cloud确认且过期的P3；
2. 已Cloud确认且过期的P2；
3. 未确认P3；
4. 经策略允许的未确认P2；
5. P1必须经过Incident和保留策略检查；
6. P0不主动淘汰，容量耗尽时进入显式保护或fail-closed。

任何Legal Hold对象不得自动删除。

---

## 22. Cloud平台设计

第一阶段采用模块化单体，避免过早微服务化。

核心模块：

- Identity/PKI；
- Asset/Inventory；
- Release/Artifact；
- Campaign/Deployment；
- Command/Receipt；
- Flight Package；
- Incident；
- Retention/Legal Hold；
- Audit；
- OTel Ingest与查询入口。

存储分工：

| 数据 | 存储 |
|---|---|
| 设备、版本、Campaign、状态 | PostgreSQL |
| 命令、回执、审计索引 | PostgreSQL |
| 固件、MCAP、黑匣子、诊断包 | S3/MinIO |
| 指标 | Prometheus/Mimir |
| 文本日志 | Loki；需要全文时使用OpenSearch |
| Trace | Tempo |

所有高风险业务写入通过PostgreSQL Outbox发布事件，禁止先发消息后写数据库。

---

## 23. 身份、权限与审计

### 23.1 身份

- 人员：OIDC + phishing-resistant MFA；
- Cloud服务：短期工作负载身份；
- RC/HC：设备mTLS证书；
- FC/MCU：设备密钥或由HC安全代理绑定；
- Release：独立代码签名密钥；
- Root：离线密钥。

### 23.2 职责分离

- 开发人员生成Candidate，但不能独立批准生产Release；
- Release Manager批准Release；
- Fleet Operator选择设备和窗口；
- Security Auditor只读审计；
- 同一人员不得同时创建、批准并执行高风险FC升级；
- Break-glass需要短期授权、双人复核和实时告警。

### 23.3 审计事件

必须记录：

- Release创建、签名、批准和撤销；
- Campaign创建、扩圈、暂停和回滚；
- 每台设备的分层回执；
- 签名、防降级和兼容性拒绝；
- 人工覆盖和Break-glass；
- 日志查询、导出、删除和Legal Hold；
- 证书签发、轮换和吊销；
- 策略和角色变更。

审计写入失败时，高风险升级操作默认拒绝。

---

## 24. 迁移方案

### 24.1 原则

- 不进行一次性直接切换；
- 先旁路观测，再接管信任和传输，最后接管安装；
- 保持旧路径可停止、可回退和可比较；
- 任一阶段不改变飞行安全authority；
- 现有Adapter先包裹、后重构，不同时改协议和设备固件。

### 24.2 阶段

#### M0：合同与安全止血

- 冻结升级Manifest、命令、回执和错误码；
- 删除日志和消息中的凭据；
- 为升级包增加SHA-256和ECDSA签名；
- 固定下载目录和文件大小上限；
- 修正升级包长度检查顺序；
- 建立现有模块和硬件BOM清单；
- 补充坏包、错硬件、重放和ARM状态负例。

#### M1：安全传输与只读观测

- HTTPS/mTLS替代FTP；
- 建立RC Gateway和设备身份；
- 新日志Agent旁路读取现有日志；
- 生成Flight Package Manifest但不改变旧上传；
- Cloud建立只读Asset、Inventory和Release目录。

#### M2：日志可靠链路

- 建立L1/L2 WAL；
- 建立`LOCAL_DURABLE`和`CLOUD_DURABLE`；
- 对象存储替代FTP日志目录；
- 引入journald、Fluent Bit和OTel；
- 引入MCAP试点；
- 验证满盘、掉电和弱网。

#### M3：Linux A/B

- 建立Boot/RootFS双Slot；
- 集成RAUC和U-Boot回滚；
- 实现健康确认；
- 在无桨台架验证掉电和Bootloop；
- 旧脚本保留为紧急回退但禁止生产远程调用。

#### M4：统一外设编排

- 将现有模块升级拆成Adapter；
- 建立Update Orchestrator；
- 增加依赖图、兼容矩阵和Saga补偿；
- 建立Campaign、Canary和暂停；
- Legacy设备实施更严格策略。

#### M5：FC/MCU签名A/B

- 引入MCUboot或等效Bootloader；
- ECDSA P-256验签；
- Security Counter；
- Test/Confirm/Rollback；
- 错签名、降级、断电和回滚矩阵。

#### M6：生产准入

- HA、备份、恢复和DR；
- 72小时soak和2倍峰值；
- On-call、告警和Runbook；
- 安全渗透和供应链评审；
- 需求、测试和Evidence Manifest闭环。

---

## 25. 测试与验收

### 25.1 升级测试

- Manifest字段、签名和兼容性合同测试；
- 坏签名、未知密钥、证书过期和吊销；
- Artifact截断、篡改、错序和重复；
- 错机型、错硬件revision和错组件；
- 版本降级和Security Counter倒退；
- 弱网、断网、重连和重复命令；
- 下载、写Slot、切Slot和健康检查各阶段断电；
- Bootloop、Watchdog和自动回滚；
- 数据Schema升级失败和回退；
- 多组件部分成功和逆序回滚；
- ARM、任务中、电量不足和温度异常拒绝；
- Campaign暂停、恢复、扩圈和撤销；
- 审计故障时高风险操作拒绝。

### 25.2 日志测试

- 高频日志和2倍峰值；
- Ring Buffer溢出和丢弃计数；
- 掉电后文件恢复；
- MCAP分片、索引和回放；
- UTC异常、时钟跳变和Boot ID；
- L1/L2掉电一致性；
- ACK超前、倒退和跨设备伪造；
- 满盘下P0/P1保留；
- Artifact SHA-256和Manifest签名；
- Cloud对象恢复；
- 凭据和隐私字段扫描；
- Incident窗口完整性；
- Retention、删除审计和Legal Hold。

### 25.3 长稳和容量

- 72小时连续运行；
- 目标最大设备数下2倍峰值；
- 最长目标断网窗口；
- 上传恢复后的积压回放；
- eMMC/SD写放大和寿命；
- CPU、RSS、线程和文件描述符；
- 日志压缩、hash和上传对HC关键进程的影响；
- OAM Agent关闭和故障时的飞行行为等价性。

---

## 26. 运维与告警

必须建立以下Runbook：

- Campaign异常暂停；
- 签名或Manifest验证失败；
- 设备连续Boot失败；
- 自动回滚失败；
- Upgrade Agent卡住；
- 证书到期或泄漏；
- L1/L2 WAL高水位；
- Cloud或对象存储不可用；
- 日志积压和Artifact验证失败；
- OTel后端故障；
- 租户隔离事件；
- 审计写入失败；
- PostgreSQL故障切换；
- 数据恢复和Legal Hold。

核心告警包括：

- 签名失败率；
- 同一硬件BOM升级失败率；
- Boot和健康确认失败率；
- 自动回滚率和回滚失败；
- 命令终态冲突；
- 升级状态长时间不前进；
- WAL水位和ACK停滞；
- P0日志丢失；
- Artifact hash不一致；
- 证书剩余有效期；
- 跨租户负例探针异常成功。

---

## 27. 风险与控制

| 风险 | 控制 |
|---|---|
| 新升级框架影响现有设备 | 先封装Adapter，保持外设协议不变 |
| A/B空间不足 | 重新规划eMMC，必要时增加存储或压缩RootFS |
| 数据Schema无法回退 | Expand/Contract、双读、备份和版本化迁移 |
| Legacy单Slot设备变砖 | 台架、维护窗口、恢复镜像和硬件淘汰计划 |
| OAM占用HC资源 | 独立进程、CPU/RSS/IO预算和限速 |
| 日志写放大损耗闪存 | 批量写、分片、压缩、寿命测量和水位策略 |
| MCAP数据量过大 | Topic allowlist、降采样、触发录制和配额 |
| 密钥泄漏 | 离线Root、HSM/TPM、安全轮换和吊销 |
| 多组件混合版本 | 兼容矩阵、Saga、最长混合窗口和逆序回滚 |
| Cloud故障影响现场 | RC L2、离线策略、命令有效期和默认拒绝 |
| 过早全量切换 | Canary、Soak、扩圈门禁和Feature Flag |

---

## 28. 开放决策

| ID | 决策 | 影响 |
|---|---|---|
| OD-01 | RK3568/RK3576最终eMMC分区和容量 | RAUC A/B |
| OD-02 | U-Boot bootcount/bootlimit现状和可修改范围 | 自动回滚 |
| OD-03 | FC/MCU Flash是否支持双Slot | MCUboot方案 |
| OD-04 | 首批纳管外设BOM和恢复能力 | Adapter优先级 |
| OD-05 | 最长断网目标和日志产生率 | L1/L2容量 |
| OD-06 | HC硬件密钥、TPM或安全区能力 | 设备身份和签名 |
| OD-07 | Cloud部署规模、RPO和RTO | HA与DR |
| OD-08 | Flight Recorder目标Schema和Decoder兼容期 | 日志演进 |
| OD-09 | MCAP首批Topic和采样Profile | ROS数据容量 |
| OD-10 | 数据分类、保留期和Legal Hold策略 | 存储成本与合规 |
| OD-11 | Legacy设备最大支持周期 | 安全准入 |
| OD-12 | Campaign审批和Break-glass组织流程 | 权限与审计 |

---

## 29. 生产完成定义

只有以下条件全部满足，才能称升级与日志系统达到生产级：

1. Linux、FC/MCU和正式Artifact均使用数字签名。
2. 生产升级链路不再依赖明文FTP和消息内密码。
3. Linux A/B通过各阶段断电、Bootloop和自动回滚测试。
4. FC/MCU通过坏签名、降级、断电和回滚测试。
5. 多组件兼容矩阵、依赖图和补偿策略经过验证。
6. 所有升级命令具备身份、审批、时效、重放保护和审计。
7. Campaign、Canary、暂停、扩圈和回滚具备真实证据。
8. Flight Recorder、MCAP、系统日志和OTel边界明确。
9. 日志中不存在口令、私钥和完整Token。
10. L1/L2和Cloud持久化ACK通过掉电与弱网测试。
11. P0/P1日志在满盘策略中受到保护。
12. Flight Package的Manifest、SHA-256、签名和恢复通过验证。
13. Retention、删除、Incident和Legal Hold可审计。
14. 72小时soak、2倍峰值和闪存寿命测试通过。
15. On-call、告警、Runbook、备份和DR演练投入运行。

升级与日志系统production-ready不等于无人机或机器人整体safe-to-operate/safe-to-fly。

---

## 30. 首批工程任务

建议首批只完成设计冻结和安全止血：

1. 建立现有所有可升级模块、硬件型号、版本和恢复能力清单。
2. 冻结`ArtifactManifest`、`UpgradeCommand`和`UpgradeReceipt`合同。
3. 定义稳定升级错误码和分层状态机。
4. 删除日志Schema、ROS消息和错误日志中的Wi-Fi/FTP密码。
5. 为现有升级包增加SHA-256和ECDSA P-256签名草案。
6. 修复升级文件大小、offset、路径和单文件校验边界。
7. 建立RAUC/U-Boot A/B最小台架原型，不接真实飞行任务。
8. 把一个低风险外设升级重构为标准Adapter参考实现。
9. 建立Flight Package Manifest参考实现。
10. 用生成数据完成L1/L2 WAL和持久化ACK原型。
11. 输出真实日志速率、峰值、断网容量和闪存寿命测量计划。
12. 建立坏签名、错硬件、重放、掉电、满盘和回滚测试矩阵。

首批任务不得解除执行器硬锁，不得据此宣称真实飞行或生产准入完成。
