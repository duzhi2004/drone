# AeroCore 日志、文件传输与整机升级目标需求

> 文档版本：v1.0-rc4
> 发布状态：对外评审稿；产品基线和批准人确认后方可转为正式版
> 文档日期：2026-08-17
> 文档性质：目标需求，不描述当前代码、实施进度或设备准入状态
> 信息分类：可供外部需求评审；不得作为软件发布、设备升级或飞行批准

本文中，“必须/不得”为强制要求，“应”为建议要求，“可”为可选能力。协议、标准、角色标识、接口字段、命令和状态值保留原英文拼写，其余内容以中文表述。

本文只规定外部可观察行为、责任边界、数据合同和验收结果。除已明确冻结的协议与文件格式外，不指定 Web 框架、数据库、队列算法、链路调度算法、升级工具或安装实现。

## 1. 目标与边界

AeroCore 必须提供统一但职责隔离的文件传输、日志管理和整机升级能力：

1. HC 对 RC 提供维护 WSS（WSS + Protobuf）、HTTPS RESTful API 和大文件服务；Web 是 HC 的服务能力，RC 是客户端。
2. HC 内部采用 ROS 2 Humble 多进程架构，以主题（topic）、服务（service）和动作（action）传递控制、状态和对象引用。
3. 飞行控制通道必须独立于维护通道；维护 WSS 不得承担或被描述为飞控实时控制通道。
4. 大文件传输必须适应丢包、断连、进程重启和长时间离线，并支持断点续传和最终完整性校验。
5. 日志必须覆盖采集、封存、检索、空间、传输、长期归档和删除生命周期。
6. 整机版本包必须支持多个部件、多个制品和不同硬件变体，并可复现、签名和验证。
7. 升级必须区分包校验、设备解析、安装门禁、暂存、激活、健康确认、提交和回滚。

以下行为明确禁止：

- 使用 WebSocket 或 ROS2 消息传输日志 ZIP、`.acpkg` 或其他大文件内容。
- 通过维护 WSS 承载遥杆/人工操纵（pilot stick/manual control）、解锁/上锁（arm/disarm）、飞行模式切换、任务执行或任意直接 FC 执行器命令；维护界面中的只读飞行状态投影不等于飞控主通道。
- 将可能影响 FC 的维护动作直接透传到 FC；此类动作必须先形成维护操作意图，经权限、安全门禁和升级/维护编排后，转换为语义受限、可审计、可幂等处理的 FC 维护意图。
- 由 RC、浏览器或版本包直接写 FC 闪存、切换 FC 槽位、替换 HC 发布版本或关闭设备门禁。
- 在版本包中携带 Shell 脚本、动态插件、WASM、私钥、设备凭据或安装时远程下载的未签名依赖。
- 把上传完成、下载完成、SHA 一致、签名有效或命令返回成功显示为“升级成功”。
- 用 TLS、版本包签名、制品哈希或 FC 安全启动中的任意一项替代其他安全层。

## 2. 系统职责与进程边界

控制（control）、传输（transfer）、日志管理（log-manager）和升级（upgrade）是四个必须独立的核心业务状态角色，不是系统进程数上限。维护接入适配器是必需的无状态边界能力，不是第五个业务状态所有者；生产部署应将其置于独立进程或等价故障域。角色可进一步拆分，但业务状态必须有唯一写入责任方。

| 角色 | 主要职责 | 不得承担 |
| --- | --- | --- |
| 飞行控制（control） | 独立飞行控制通道、FC 链路、飞行命令、实时飞行状态和飞行门禁事实 | 维护 WSS、大文件 I/O、日志文件写入、升级任务持久化 |
| 维护接入适配器 | HC 的 WSS + Protobuf 维护端点、维护操作受理/反馈、事件/进度/告警推送和 REST 状态重同步引导 | 飞控实时控制、大文件内容、业务权威状态或绕过门禁直接影响 FC |
| 传输（transfer） | HC HTTPS、上传/下载会话、不可变对象、目录索引、限流和包验证 | 激活版本、写 FC、修改已封存日志 |
| 日志管理（log-manager） | 日志会话、打包、原子封存、隔离区、空间和保留策略 | 对外提供大文件服务、执行升级、拥有 FC 控制链路 |
| 升级（upgrade） | 解析器、升级事务、事务日志/发件箱、安装处理器、健康确认和回滚 | 接收大文件内容、写日志目录、执行包内代码 |
| RC 客户端 | 提交操作意图、展示权威状态、保存本地下载检查点 | 充当设备状态权威、保存发布私钥、绕过门禁 |
| 版本包工具 | `build`、`inspect`、`verify`、`resolve`、`sign` 以及模式/目录管理 | 连接设备执行安装、保存设备秘密 |
| 长期证据存储适配器 | 上传已封存日志并返回受认证的长期存储回执 | 修改日志内容、直接决定删除资格 |

| ID | 核心要求 |
| --- | --- |
| SYS-001 | 四个核心业务状态角色和维护接入适配器必须采用独立进程或等价故障域；维护接入、传输、日志管理和升级进程重启不得中断飞行控制通道或 FC 链路。 |
| SYS-002 | HC 应用层跨进程接口必须使用版本化 ROS2 主题/服务/动作；大对象只传 ID、SHA、大小和受限元数据。 |
| SYS-003 | 飞行控制热路径必须与维护 WSS、HTTPS/TLS 握手、压缩、校验、大文件 I/O 和外部客户端处理隔离，并使用独立的有界队列和资源预算。 |
| SYS-004 | 所有队列、连接、会话、重试、工作进程、任务和事务发件箱（outbox）必须有容量上限和过载处理规则。 |
| SYS-005 | ROS2 只承担通信，不充当持久化存储；任务和传输状态由目录索引、事务日志或检查点恢复。 |
| SYS-006 | Web/API 适配层不得成为日志包、上传会话、升级计划或升级事务的权威状态源；页面、连接或适配层重启不得改变业务事实。 |
| SYS-007 | 飞行控制与维护/文件业务允许共享物理 IP 承载，但必须具备独立端点、连接/会话、认证授权上下文、队列、QoS/资源预算和逻辑故障域；不得共享会产生队头阻塞、反压传播或级联重启的连接生命周期。 |
| SYS-008 | 调度和资源仲裁必须保证飞行控制最高优先级；维护通道的故障、拥塞、认证失败、重连、限流或升级不得降低飞控可用性，也不得使飞控指标超出产品基线。 |

ROS2 接口约定：

- 主题用于有界数据流、状态和事件；服务用于短请求；动作用于打包、安装、回滚等长事务。
- 日志入口使用有界 `best_effort`，通过会话代次、序号和丢弃/缺口计数说明完整性，不得反压控制链路。
- 门禁和任务状态使用可靠 QoS；QoS、命名空间、单位、时间语义和模式版本必须随接口冻结。
- 核心节点必须提供生命周期和就绪状态，以及 `diagnostic_msgs/DiagnosticArray` 诊断，并使用 SROS2 最小权限策略。
- 安全门禁的新鲜度使用同一启动代次/源实例下的单调时间；UTC/ROS 时间只用于关联和展示。

## 3. 飞行控制、维护 WSS、RESTful API 与 HTTPS 通信需求

对外只有两个逻辑平面：独立飞行控制通道，以及 HC 维护通道。HC 维护通道内部再按负载拆成 WSS + Protobuf 维护消息、HTTPS RESTful 资源接口和 HTTPS 大文件接口；这些子通道共享对象与权限语义，但不共享连接、队列或持久状态。

| 通道 | 选择理由 | 承载内容 |
| --- | --- | --- |
| 独立飞行控制通道 | 飞行控制需要独立的时延、可靠性、资源和故障隔离约束 | 飞行命令、必要的实时状态和确认；不承载日志/升级管理或大文件 |
| WSS + Protobuf 维护通道 | 双向、低开销、可主动推送，避免频繁轮询 | 日志/升级维护操作、ACK/NACK、状态变化、进度和告警；不承载飞控实时控制 |
| 维护通道 HTTPS RESTful API | 资源模型清晰，便于 RC、调试工具和第三方系统调用 | 目录查询、会话、版本包检查、升级任务、日志和审计操作 |
| 维护通道 HTTPS 文件接口 | 原生支持流式消息体、Range、ETag、缓存控制和成熟工具 | 日志包、诊断包、版本包及制品 |
| ROS2 | 支持强类型接口、QoS、生命周期、诊断和追踪 | HC 内部控制、状态、事件和对象引用 |

大文件若走 WebSocket 或 ROS2，需要重复实现分块、续传、背压和大消息内存管理；维护事件若全部改成 HTTP 轮询，则会增加无线请求量和状态竞争。因此目标架构固定为“独立飞行控制通道 + WSS + Protobuf 维护通道 + HTTPS RESTful 管理接口和文件通道 + ROS2 内部消息面”。日志和升级的持久化资源必须可通过 RESTful API 查询；维护 WSS 断线后以权威资源状态完成重同步。逻辑独立不强制使用不同网卡或物理网络，但共享物理 IP 承载时仍必须满足端点、会话、安全、队列、QoS 和故障隔离要求。

| ID | 通信要求 |
| --- | --- |
| NET-001 | 维护 WSS 只允许有大小上限的维护操作/事件消息；操作携带请求/幂等 ID、操作身份、截止时间和模式版本，不得承载飞控实时控制命令。其飞行状态仅为带来源、新鲜度和质量标识的只读投影，不构成飞控主通道合同。 |
| NET-002 | 维护 WSS 的 ACK 只表示接受或拒绝；长事务结果必须从 HC 权威操作/事务状态读取。 |
| NET-003 | 维护 WSS 重连后必须重新认证并进行状态重同步，不自动重放过期操作，不因断连删除上传或任务。 |
| NET-004 | 上传使用持久化会话，绑定身份、对象类型、预期大小/SHA、分块配置和过期时间；每个数据段携带偏移、长度和分块 SHA。 |
| NET-005 | 完成提交必须幂等校验数据范围完整性、总长度和整包 SHA，再原子发布不可变对象；未完成会话不得进入普通目录索引。 |
| NET-006 | 下载支持 `HEAD`、`Range`、`If-Range`、强 ETag、`206`、`416`、检查点和最终 SHA-256。 |
| NET-007 | 非回环链路必须使用 WSS/HTTPS + mTLS；WS/HTTP 只允许显式回环调试环境。 |
| NET-008 | 私有无线只要提供 IP 即可直接使用；原始字节链路必须先通过 PPP/TUN/TAP 或等价方式提供 IP。 |
| NET-009 | 飞行控制流量必须具有最高优先级，维护操作/关键事件次之，大文件流量最低；维护和文件业务必须支持限速、暂停、恢复、连接/速率/空间配额和稳定错误码。 |
| NET-010 | RESTful API 使用版本化资源路径（如 `/api/v1`）、标准 HTTP 方法和状态码；查询支持分页/过滤，可并发修改的资源支持条件更新，错误响应包含稳定错误码和关联 ID。 |
| NET-011 | 创建上传会话、日志包、设备解析计划和升级事务等长操作必须支持幂等键，并返回可查询的操作/事务 ID；异步受理使用 `202 Accepted`。 |
| NET-012 | RESTful API 必须发布 OpenAPI 规范，维护 WSS 必须发布机器可读的 Protobuf 操作/事件合同；二者共用认证、RBAC、审计、对象 ID 和状态语义，不得为调试接口设置旁路权限。 |
| NET-013 | RC/Web 对日志和升级持久状态的变更必须以 RESTful API 为规范入口；维护 WSS 可受理维护操作意图并提供事件/反馈，但必须映射到相同资源、权限、幂等和状态语义，不得形成第二套业务写入合同。 |
| NET-014 | 维护 WSS 事件必须携带流实例、单调序号、资源版本和操作/事务 ID；重连后支持有界补发，无法补齐时明确要求客户端通过 RESTful API 重新同步。 |
| NET-015 | HC 必须提供受认证的能力发现，声明支持的 API、事件/模式版本、对象上限、传输能力和弃用状态；RC 不得根据软件版本字符串猜测能力。 |
| NET-016 | 同一主版本内的接口演进必须保持兼容；客户端遇到未知响应字段或状态时不得崩溃、误报成功或执行未授权动作。破坏性变更必须发布新主版本和迁移期限。 |
| NET-017 | 飞行控制、维护操作/事件和大文件流量必须具有独立、有界的资源预算；维护和大文件流必须可被飞行控制抢占，过载时优先限制维护业务并暂停或拒绝大文件任务。 |
| NET-018 | 文件传输速率、分段和并发必须受当前有效带宽、时延、重传、飞行控制队列和飞行状态约束；链路事实过期或飞控服务等级恶化时必须自动降速或暂停维护/文件业务，并采用稳定恢复条件。 |
| NET-019 | 分段参数和并发上限必须由双方在设备允许范围内协商并固化到会话；服务端确认的偏移或缺失范围是恢复依据，客户端“已发送”不代表服务端已持久化。 |
| NET-020 | 底层 HTTP/TLS 版本、链路或安全回退发生变化时，REST 资源、文件完整性和任务状态语义必须保持一致；回退不得降低认证、授权或防重放要求。 |
| NET-021 | 长操作必须提供可查询的受理、执行、成功、失败和取消语义，以及当前阶段、结果或稳定错误；进度未知时不得伪造百分比，维护 WSS 进度不得覆盖最终权威结果。 |
| NET-022 | 飞行控制与维护/文件业务可共享同一物理 IP 接口或承载链路，但必须使用独立网络端点、连接/会话、认证授权上下文、收发队列、QoS/等价优先级和逻辑故障域；不得复用单一会话形成队头阻塞。 |
| NET-023 | 在维护 WSS/HTTPS 拥塞、认证风暴、反复重连、大文件重传、进程崩溃或升级期间，飞行控制必须保持最高调度和带宽保障，不得发生断连、队列耗尽、级联重启或飞控指标违约。 |
| NET-024 | 任何可能改变 FC 状态的维护请求只能作为维护操作意图进入权限校验、安全门禁和升级/维护编排；通过后只能转换为允许列表内、参数有界、可审计且可幂等处理的 FC 维护意图，不得透传任意 FC 命令。 |

TCP 保证单次连接内的有序可靠字节流；应用会话负责连接中断、进程重启和长离线恢复。产品配置基线必须分别冻结 MTU/MSS、飞行控制带宽/时延预算、维护业务预算和文件传输上限，并通过丢包、RTT、抖动、断连和资源竞争测试验证。

## 4. 整机版本包 `.acpkg`

### 4.1 组合模型

```text
版本包目录
  -> 多个 .acpkg 发布版本
     -> 多个必选/可选部件
        -> 多个硬件变体
           -> 多个载荷制品
```

- 部件可以表示 FC、HC、RC、NavX、载荷或配置。
- 通用包（`universal`）可包含多个平台；已解析包（`resolved`）只绑定一个可信设备快照。
- 必选部件必须唯一匹配硬件变体；可选部件未匹配时可跳过，选入计划后必须成功。
- 设备能力来自本机可信配置和硬件清单，不能被包内声明替代。
- 每个硬件变体必须声明部署责任设备、安装处理器、依赖、兼容合同、激活顺序和回滚方式。

| ID | 发布与传输要求 |
| --- | --- |
| PKG-001 | 整机发布身份、制品身份、物理归档身份和传输会话身份必须分离；归档重打包、签名集合变化或网络重传不得混淆整机发布身份。 |
| PKG-002 | 面向已知设备传输时，不得强制通过受限无线链路发送未被设备计划选中的硬件变体或可选制品；最终安装集合必须与受信设备计划完全一致。 |
| PKG-003 | 发布授权必须能够拒绝过期、回滚、冻结、混合版本、错误设备和未授权发布者；版本包签名不得代替设备本地信任根、密钥轮换和发布角色分离。 |
| PKG-004 | SemVer 用于表达功能版本；整机发布和可独立升级部件必须使用作用域明确的单调安全序号防止降级。恢复旧功能也必须经过新的授权，不得简单降低安全序号。 |
| PKG-005 | 每个发布版本必须携带或受签名清单引用机器可读的 SBOM 和构建来源证明，并以大小、SHA 和模式版本固定；缺失或不匹配时不得进入生产发布。 |

### 4.2 格式定义

| 属性 | v1 要求 |
| --- | --- |
| 扩展名 | `.acpkg` |
| 媒体类型 | `application/vnd.aerocore.system-package+zip`；正式注册前不能只依赖请求头识别 |
| 容器 | 确定性 ZIP64，条目存储方式为 `STORE` |
| 清单 | 第一项为 `manifest.json`；模式为 `aerocore_system_package_v1` |
| 规范化方式 | UTF-8 RFC 8785 JCS |
| 版本包 ID | `sha256(JCS(manifest))`，表示发布内容和安装意图 |
| 归档 SHA | 完整 `.acpkg` 的 SHA-256，表示具体归档字节 |
| 签名 | 分离式 Ed25519 清单签名，支持密钥 ID、撤销和门限签名策略 |

```text
manifest.json
signatures/manifest-signatures.json
artifacts/sha256/<prefix>/<full-sha256>
metadata/sbom/...
metadata/release-notes/...
```

SHA-256 使用 64 位小写十六进制；对外标识使用 `sha256:<hex>`。版本包 ID 和归档 SHA 是派生值，不得写入自身所哈希的内容形成自引用。改变签名集合不改变版本包 ID，但会改变归档 SHA；版本包索引必须允许一个版本包 ID 对应多个归档/签名集合。

清单至少包含：

- 整机 `name`、SemVer 版本、发布通道、模式、RFC 3339 UTC 创建时间和按数值比较的回滚序号。
- 平台/配置标识及其 SHA-256。
- 部件版本、源码/来源证明以及 `requires/provides` 兼容合同。
- 硬件变体选择器、安装处理器及版本、能力、激活顺序、回滚方式和载荷角色。
- 制品路径、媒体类型、大小和 SHA-256；可展开载荷还需声明展开大小、文件数上限和受签文件清单。
- 安装策略、发布说明、SBOM 和来源证明引用。

清单必须固定所有载荷和元数据条目，不得接受未引用条目。ZIP 和内层载荷都必须拒绝路径穿越、绝对路径、符号链接/硬链接/设备文件、重复或规范化冲突、越权权限/所有者以及超限展开。版本包不得携带安装处理器代码，设备只执行本机内置且由允许列表管理的处理器。

### 4.3 构建与验证

CLI 和 GUI 必须复用同一强类型核心库，提供 `build`、`inspect`、`verify`、`resolve`、`sign` 以及模式和目录管理。构建配置与清单分离，不允许通过 URL 下载、执行命令或访问源码根目录外的路径。

标准流程为：

1. 校验构建配置、平台配置、部件/硬件变体/依赖和策略目录。
2. 流式计算制品大小/SHA，并执行安装处理器专项检查。
3. 生成 JCS 清单和确定性未签名归档。
4. 在隔离签名环境使用发布签名密钥生成禁止覆盖的新签名包。
5. 重新打开最终文件，依次校验容器、清单、信任、内容、设备解析和安装前门禁。

离线工具可完成容器、清单、信任和内容校验；设备必须基于本机信任库、设备配置、实时门禁和回滚资源重新验证。设备侧 `resolve` 只生成不可变计划，不重写原包；离线生成 `resolved` 包时必须产生新的版本包 ID 并重新签名。

## 5. 升级事务需求

版本包、设备计划和升级事务是三个不同对象：

```text
版本包对象（传输进程负责）
  uploading -> uploaded -> structurally_valid
            -> signature_verified -> content_verified

设备绑定计划（升级进程负责）
  resolving -> resolved -> preinstall_check -> eligible
  resolved/eligible --设备事实或策略变化--> stale

升级事务（升级进程负责）
  created -> staged -> activating -> health_check -> committed
  activating/health_check --失败--> rollback -> rolled_back
  rollback --失败--> failed_safe
```

| ID | 升级要求 |
| --- | --- |
| UPG-001 | 上传、验证、解析、安装和回滚必须使用不同 API 和权限；包验证成功不等于设备可安装。 |
| UPG-002 | 已解析计划必须绑定版本包 ID、归档 SHA、设备事实哈希、信任策略版本、安装处理器/版本和部件顺序。 |
| UPG-003 | 事务状态和待发送指令必须在同一次持久化提交中写入事务日志/发件箱；投递可重放，接收方必须去重。 |
| UPG-004 | 安装处理器必须以事务 ID 保证幂等；进程重启和超时重试不得重复刷写、切换或提交。 |
| UPG-005 | 激活前及每个不可逆步骤前重新检查包标识、签名信任和飞行/解锁/任务、电源、空间、温度等门禁。 |
| UPG-006 | 所有计划必需部件必须先暂存，再按依赖顺序激活；失败时按反序回滚。 |
| UPG-007 | 健康检查必须读回真实版本、槽位、服务标识和关键功能；全部责任方确认后才能显示 `committed`。 |
| UPG-008 | FC 升级必须由 FC 本地 A/B 启动控制状态机完成 `pending/testing/confirmed/rollback`；HC ACK 或写入完成不代表成功。 |
| UPG-009 | 回滚失败进入 `failed_safe` 并停止自动推进，输出可审计的人工恢复信息。 |
| UPG-010 | 跨 HC、FC、RC、NavX 或载荷的升级不得宣称分布式强原子提交；每个参与方必须独立报告暂存、激活、健康、提交和回滚事实。 |
| UPG-011 | 不支持暂存、健康读回或回滚的部件必须在计划和审批中明确标识，默认不得进入生产整机事务；经批准的例外必须最后激活并提供人工恢复路径。 |
| UPG-012 | A/B 或发布版本回滚必须同时考虑持久数据、配置和模式兼容性；数据迁移不可逆或旧版本不可读取时，不得声称具备完整回滚能力。 |
| UPG-013 | 每个参与部件必须提供可认证且带新鲜度的已安装版本、制品标识、槽位和健康报告；HC 不得仅根据下发命令或缓存推断整机版本。 |

升级对象、`active/previous-good` 发布版本、待确认 FC 槽位、任务和法律保全引用不得被垃圾回收。上传、暂存和回滚空间必须在产生设备副作用前预留，且不得通过删除受保护日志临时腾挪。

## 6. 日志管理需求

### 6.1 分类与格式

| 类别 | 内容 | 权威责任方 |
| --- | --- | --- |
| 飞行/会话证据 | FC 二进制/黑匣子/文本、HC 结构化事件、任务、配置和版本信息 | 日志管理进程 |
| 诊断包 | 指定时间窗的服务日志、状态、指标和诊断证据 | 日志管理进程受控收集 |
| 升级审计 | 包、门禁、操作人、安装处理器、健康检查和回滚事件 | 升级事务日志/发件箱 |
| 安全审计 | 认证、授权、拒绝、信任策略和敏感访问 | 安全审计责任方 |

四类数据必须使用独立权限、目录索引和保留策略。飞行日志包不得隐式包含秘密信息或完整系统日志，日志也不能替代升级事务权威状态。

目标 `flight_package_v2` 清单使用 JSON Schema Draft 2020-12 和 JCS，至少记录：

- 飞行/会话/飞行器/任务标识、FC 启动代次、开始/结束时间和可信时间状态。
- FC/HC 版本、参与会话的 RC/部件版本或缺失原因。
- 整机版本包 ID、部件制品、FC 槽位、配置/场景标识。
- 完整性、缺口/丢弃计数、制品清单和每个文件的 SHA-256。

日志包 ID 为规范化清单的 SHA-256，归档 SHA 为最终 ZIP 的 SHA-256。归档 SHA 必须放在 ZIP 外的旁文件/目录索引中，不能写入 ZIP 内形成循环哈希；对应字段名分别为 `log_package_id` 和 `archive_sha256`。

```text
<log-package-id>/
├── manifest.json
├── checksums.txt
├── artifacts/...
└── exports/
    ├── flight_package.zip
    ├── flight_package.zip.sha256
    └── flight_package.zip.evidence.json  # 需要真实性时
```

诊断包使用独立的 `aerocore_diagnostic_bundle_v1` 模式、ID、命名和目录类型。日志包和诊断包都必须确定性、有界内存构建；HTTPS 请求不得现场生成 ZIP。需要真实性时，由独立的证据签名密钥签署日志包/诊断包 ID、归档 SHA、大小和导出配置，不得复用发布签名密钥。

### 6.2 生命周期、空间和传输

```text
规范日志包/诊断包（日志管理进程）
  active -> sealing -> sealed -> retention_eligible -> pruned
              \-> quarantine

每个客户端下载（传输进程/RC）
  created -> transferring -> completed/paused/expired/failed

每个长期存储目的地的归档回执（证据存储适配器）
  pending -> uploading -> receipt_received -> archive_verified
```

| ID | 日志要求 |
| --- | --- |
| LOG-001 | `active` 只允许日志管理进程写，传输进程只读 `sealed`；整个日志包目录必须在同一文件系统原子发布。 |
| LOG-002 | 会话必须绑定飞行器/任务、FC 启动代次、源实例、会话代次和开始/结束原因；重启和迟到消息不得合并不同会话。 |
| LOG-003 | 日志入口必须有界、非阻塞并记录缺口/丢弃；需要无损的原始证据由 FC 黑匣子或专用源端缓冲保证。 |
| LOG-004 | 封存必须完成清单/校验和、ZIP、旁文件、`fsync` 和原子重命名；失败进入 `quarantine`，进入 `sealed` 后不得修改。 |
| LOG-005 | 新会话前按最坏速率、时长、归档开销和字节数/inode 预留空间；活动会话具有严格的字节数、时长和 inode 上限。 |
| LOG-006 | 使用高/低水位并保留根文件系统、升级和系统安全余量；无可删除对象时拒绝新日志会话，但不得停止控制链路。 |
| LOG-007 | `active`、`sealing`、`quarantine`、最新包、固定保留、法律保全和待归档证据不得被普通保留策略删除。 |
| LOG-008 | `quarantine` 具有独立配额、告警和授权处置流程；达到配额时不得覆盖既有证据。 |
| LOG-009 | 传输目录索引提供对象/日志包/诊断包 ID、类型、时间、归档大小/SHA、完整性、保护和长期存储状态，不暴露 HC 文件路径。 |
| LOG-010 | RC 下载使用 Range/ETag/检查点/最终 SHA；断连后不得拼接不同归档。下载成功不自动删除 HC 原件。 |
| LOG-011 | 长期证据存储适配器只上传已验证的 `sealed` 归档，保存目的地检查点，并取得包含 ID、归档 SHA、远端对象 ID 和验证时间的认证回执。 |
| LOG-012 | 只有满足产品配置规定的全部必需长期存储回执、保留期及固定保留/法律保全条件，日志管理进程才能标记 `retention_eligible`。 |
| LOG-013 | 对外导出必须区分摘要、受限诊断和完整证据；每种导出具有独立清单、ID、完整性状态和权限，摘要或抽样结果不得标记为完整日志。 |
| LOG-014 | 在不改变已封存原件的前提下，系统必须支持按时间窗、模块、严重级别或制品选择受控导出，避免弱链路被迫下载无关数据。 |
| LOG-015 | 结构化日志、诊断和审计必须使用版本化模式，并关联飞行器、任务、会话、启动代次、部件、版本、操作/事务、单调时间、可信 UTC 状态和数据分级。 |
| LOG-016 | OTel、指标和可视化只作为运行观测投影，不得成为日志完整性、升级事务状态、保留资格或删除授权的唯一事实源。 |

日志容量按“最坏产生速率 × 最长离线时间 + 归档/恢复开销 + 安全余量”确定。秘密信息、私钥、令牌、口令和完整证书不得进入日志；下载、导出、保全、紧急覆盖和删除必须审计。普通文件删除不得描述为安全擦除。

## 7. Web、权限与安全

HC Web 界面至少提供服务状态、日志目录/续传下载、版本包上传/检查、设备兼容计划、升级门禁、逐部件进度、健康检查、回滚和审计。维护 WSS 承载维护操作、反馈、事件和进度，其中所有持久状态变更和长操作必须映射到 RESTful API 的同一权威资源并返回操作/事务 ID；页面刷新或事件缺口后从 HC 权威 RESTful API 恢复。Web 不得把只读飞行状态投影展示为飞控主通道，也不得提供本章禁止的飞控命令入口。

页面不得在浏览器端判断门禁、直接调用安装处理器、修改 `sealed` 内容，或把进度百分比推断为成功。开发/仿真配置必须持续显示 `DEVELOPMENT`/`SIMULATED`，使用独立测试 PKI 和数据域，不得导出为生产证据。

权限至少分为：

- 观察员（`observer`）：只读普通状态。
- 维护人员（`maintainer`）：文件传输和受限诊断。
- 审批人员（`approver`）：批准升级、回滚和受控证据操作。
- 设备管理员（`device-admin`）：设备身份、信任策略和紧急恢复。
- 审计人员（`auditor`）：独立只读审计/证据，不执行设备变更。

生产激活默认要求两个不同身份确认，上传者不得批准自己上传的生产版本包；具体审批人数和规则由产品配置冻结。紧急越权恢复（break-glass）必须独立授权、限时并完整审计。

安全要求：

- HC Web 服务限制 Host/Origin/CORS、请求数量、请求头、消息体、连接和速率，并防 CSRF 和重放攻击。
- 可能被网络重放的传输优化不得用于创建任务、激活、回滚、删除或其他有副作用的请求。
- RC 原生应用、WebView 和浏览器均使用受管身份；浏览器不能直接使用设备证书时通过本机受保护代理。
- mTLS CA、发布签名密钥、证据/审计密钥、存储加密密钥和 FC 安全启动密钥必须分离用途和生命周期。
- 信任根、撤销和门限签名策略来自设备本地信任库，不信任包内公钥或客户端提交的角色。
- 运营/受限证据按数据保护配置执行静态保护；安全/升级审计必须追加写入、可检测篡改并独立保留或远端归档。

## 8. 产品基线与验收

正式实现前，每个产品/硬件配置基线必须冻结：

| 参数域 | 必须确定 |
| --- | --- |
| ROS2 | Humble 补丁版本、RMW、命名空间、IDL 版本、QoS、执行器和 SROS2 策略 |
| Web/PKI | RC 客户端形态、RESTful API/OpenAPI、维护 WSS 版本、端口、TLS/密码套件、身份签发、轮换/撤销和限流，以及与飞行控制通道独立的端点、会话和认证授权边界 |
| 接口治理 | REST 对象、维护 WSS 的 Protobuf 操作/事件、独立飞行控制接口、错误目录、幂等保留期、能力发现、兼容/弃用和状态重同步规则 |
| 传输 | 最大文件、分段协商范围、并发/队列、超时、重试、MTU/MSS、流量等级、飞行控制带宽预留、维护业务预算、QoS/故障域和暂停/恢复条件 |
| 版本包 | 部件/设备责任范围、平台/能力/安装处理器目录、发布角色、签名门限、信任根、安全序号和回滚策略 |
| 升级 | 编排范围、审批规则、维护/飞行/电源/空间/温度门禁、健康观察窗口、重试和人工恢复 |
| 日志 | 产生速率、会话上限、归档算法、水位、隔离区配额、保留期、必需长期存储目的地和回执合同 |
| 性能 | 飞行控制延迟与尾延迟、UART 丢弃、CPU/RSS/I/O、有效吞吐、重传字节、恢复时间、归档/校验吞吐和长期容量 |

必须完成以下验收：

1. 在带宽限制、随机/突发丢包、RTT/抖动、乱序、断连和链路切换下，上传下载可恢复，重复/过期维护操作无副作用；维护 WSS 和文件流开启、拥塞、断开或重连前后，独立飞行控制通道的延迟尾部、心跳、可用性和队列水位服务等级目标（SLO）均达标。
2. 相同输入构建字节完全一致的 `.acpkg`；非法 ZIP/JCS、路径、哈希、签名、选择器和载荷全部默认拒绝（fail closed）。
3. `universal` 包对每台目标设备生成唯一计划；不匹配、多匹配、能力不足和门禁变化均不可激活。
4. 每个持久化点终止进程后，事务日志/发件箱、安装处理器和事务可幂等恢复；健康检查失败能够回滚或进入 `failed_safe`。
5. `active` 日志不可下载，`sealed` 日志原子且不可变；隔离区、空间水位、保留策略和长期存储回执行为可重复验证。
6. x86-64 参考配置使用相同 RESTful API、维护 WSS、模式和状态语义完成模拟上传、下载、封包、升级、回滚和故障恢复，并使用独立端点模拟飞行控制通道，明确标记为非目标机证据。
7. 目标设备完成 eMMC ENOSPC/EIO/只读/掉电、无线、冷启动、回滚、容量和资源竞争验证。
8. mTLS/RBAC、签名撤销、职责分离、敏感数据保护和审计防篡改形成闭环。
9. OpenAPI、维护 WSS 的 Protobuf 操作/事件合同、能力发现、兼容性和错误目录通过自动一致性测试；事件丢失或超出补发窗口时，RC 能通过 RESTful API 恢复相同权威状态。
10. 过期授权、回滚序号、混合版本、错误设备、无关硬件变体和来源证明不匹配均被拒绝；受限链路只传输设备计划需要的制品时仍可完成全链路校验。
11. 在飞行控制与维护/文件业务共享物理 IP 承载的测试中，可验证独立端点、连接/会话、认证授权、队列、QoS 和逻辑故障域；注入维护拥塞、认证失败、重连风暴、大文件重传和维护进程崩溃后，飞控仍满足第 1 项指标且不发生级联重启。
12. 通过维护 WSS 提交遥杆/人工操纵、解锁/上锁、飞行模式、任务执行和直接执行器命令时必须拒绝并审计；允许影响 FC 的维护动作必须可证明先经过权限、安全门禁和编排，并仅生成允许列表内、参数有界且幂等的 FC 维护意图。

本文转为正式基线前，必须完成产品、系统架构、安全、FC、HC、RC 和运维评审；发布数据模式、IDL/QoS、媒体类型和兼容策略；将强制要求映射到测试或安全分析，并登记批准版本及适用产品/硬件。
